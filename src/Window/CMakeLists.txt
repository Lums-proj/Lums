file(GLOB SRC "*.cpp")
if (APPLE)
    find_library(COCOA_LIB Cocoa)
    file(GLOB OS_SRC "MacOSX/*.cpp" "MacOSX/*.m" "MacOSX/*.mm")
    set(OS_LIB ${COCOA_LIB})
endif()
list(APPEND SRC ${OS_SRC})
add_library("lums-window" SHARED ${SRC})
target_link_libraries("lums-window" "lums-core" ${OS_LIB})
